11_graph_algorithms — graph algorithms
=====================================

This module contains implementations and explanations for classic graph
algorithms: BFS, DFS, shortest path (unweighted via BFS), connected
components, cycle detection, topological sort, and Dijkstra.

Each subfolder contains runnable Python examples and tests under `tests/`.
