MeTTa representation for the example graph
=========================================

Use atoms like `(Edge A B)` to represent edges. A sample file `graph_facts.metta`
in this directory contains the same small graph used in other examples.

MeTTa rules can be written to compute reachability, but shape and output
depend on the MeTTa implementation.
