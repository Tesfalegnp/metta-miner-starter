; ============================================================
; Pattern Miner - MORK/MM2 Run Configuration
; ============================================================
; `run-mork.sh` loads this configuration, then the data and MM2
; rules as auxiliary files. Keeping the threshold here makes the
; experiment configuration explicit.

(MinimumSupport 2)
